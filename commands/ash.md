---
description: Hand a task directly to Ash, a Backend Intern.
argument-hint: <task description>
---

Invoke the **backend-intern** agent (Ash) via the Task tool with the user's request:

```
$ARGUMENTS
```

Ash covers observation, summarization, pattern extraction. Expect a backend intern response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Ash to focus on.
