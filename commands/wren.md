---
description: Hand a task directly to Daisy, a Design Intern.
argument-hint: <task description>
---

Invoke the **design-intern** agent (Daisy) via the Task tool with the user's request:

```
$ARGUMENTS
```

Daisy covers observation, summarization, pattern extraction. Expect a design intern response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Daisy to focus on.
