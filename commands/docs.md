---
description: Hand a task directly to Scribe, a Technical Writer.
argument-hint: <task description>
---

Invoke the **tech-writer** agent (Scribe) via the Task tool with the user's request:

```
$ARGUMENTS
```

Scribe covers docs-as-code, Markdown, MDX, Mintlify, Docusaurus, API references. Expect a technical writer response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Scribe to focus on.
