---
description: Hand a task directly to Maya, an UI Designer.
argument-hint: <task description>
---

Invoke the **ui-designer** agent (Maya) via the Task tool with the user's request:

```
$ARGUMENTS
```

Maya covers Figma, Sketch, design tokens, component libraries, Tailwind, shadcn/ui. Expect a ui designer response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Maya to focus on.
