---
description: Hand a task directly to Mesh, a Network Engineer.
argument-hint: <task description>
---

Invoke the **network-engineer** agent (Mesh) via the Task tool with the user's request:

```
$ARGUMENTS
```

Mesh covers DNS, CDN, Cloudflare, BGP, VPN, load balancing, and more. Expect a network engineer response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Mesh to focus on.
