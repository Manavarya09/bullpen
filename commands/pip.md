---
description: Hand a task directly to Pip, a Frontend Intern.
argument-hint: <task description>
---

Invoke the **frontend-intern** agent (Pip) via the Task tool with the user's request:

```
$ARGUMENTS
```

Pip covers observation, summarization, pattern extraction. Expect a frontend intern response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Pip to focus on.
