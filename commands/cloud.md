---
description: Hand a task directly to Bash, a Cloud Architect.
argument-hint: <task description>
---

Invoke the **cloud-architect** agent (Bash) via the Task tool with the user's request:

```
$ARGUMENTS
```

Bash covers AWS, GCP, Azure, Cloudflare, Vercel, Fly.io, and more. Expect a cloud architect response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Bash to focus on.
