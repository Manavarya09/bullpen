---
description: Hand a task directly to Kernel, a Systems Engineer.
argument-hint: <task description>
---

Invoke the **systems-engineer** agent (Kernel) via the Task tool with the user's request:

```
$ARGUMENTS
```

Kernel covers Linux, bash, systemd, perf, eBPF, networking, and more. Expect a systems engineer response ending with the universal 5-options + ⭐ pick format.

If the user provided no arguments, ask what they'd like Kernel to focus on.
